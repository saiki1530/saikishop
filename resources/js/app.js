import './bootstrap';

import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();

    // function openModal(modalName) {
    //     var modal = document.querySelector(`[name="${modalName}"]`);
    //     if (modal) {
    //         modal.classList.add('active');
    //     }
    // }

    // function closeModal(modalName) {
    //     var modal = document.querySelector(`[name="${modalName}"]`);
    //     if (modal) {
    //         modal.classList.remove('active');
    //     }
    // }

    // document.querySelector('.delete-account-button').addEventListener('click', function() {
    //     openModal('confirm-user-deletion');
    // });

    // document.querySelector('.cancel-button').addEventListener('click', function() {
    //     closeModal('confirm-user-deletion');
    // });

    // document.addEventListener('click', function(event) {
    //     if (event.target.classList.contains('confirm-user-deletion-modal')) {
    //         closeModal('confirm-user-deletion');
    //     }
    // });


